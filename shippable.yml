language: python

before_script:
  - shippable_retry sudo add-apt-repository -y ppa:george-edison55/precise-backports
  - shippable_retry sudo apt-get update -y
  - shippable_retry sudo apt-get install -y gcc cmake valgrind

script:
  - ctest -VV --output-on-failure -S CTestRun.cmake
box:
    id: alpine
    cmd: /bin/sh

build:
  steps:
    - script:
        name: install dependencies
        code: |
          apk update
          apk add build-base python valgrind
          apk add git perl

    - script:
        name: build code
        code: |
          git submodule update --init --recursive
          python ci-tools/install_cmake.py
          python ci-tools/run_ctest.py
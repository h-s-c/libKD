box: debian

build:
  steps:
    - script:
        name: install dependencies
        code: |
          apt-get update -y
          apt-get install -y gcc cmake

    - script:
        name: build code
        code: |
          ctest -VV --output-on-failure -S CTestRun.cmake